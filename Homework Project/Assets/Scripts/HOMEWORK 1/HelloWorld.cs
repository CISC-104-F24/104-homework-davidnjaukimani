using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, world!");
        Debug.LogWarning("I should fix my sleep schedule.");
        Debug.LogError("David flew too close to the sun");
        //Author: David Njau Kimani
        /*
         * What do you think the most challenging aspect of this class is (or the one that people tend to struggle with/should pay the most attention to)
         * A visually simple roguelike with procedurally generated items, characters and levels
         * I hope to learn more about procedural generation
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
